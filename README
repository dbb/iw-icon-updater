dbbolton (Daniel Bolton)
         <danielbarrettbolton at gmail>

Use this script (as root) to replace the default Iceweasel icons. First look
over the 'Options' section at the top of the script file.

